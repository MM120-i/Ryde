import React from "react";
import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const _layout = () => {
  return (
    <SafeAreaView>
      <Text>app/(root)/(tabs)/_layout.tsx</Text>
    </SafeAreaView>
  );
};

export default _layout;
